<?xml version="1.0" encoding="utf-8"?>
<network-security-config
	xmlns:tools="http://schemas.android.com/tools">
	<!-- Some manga sources serve pages/covers from plain-HTTP CDNs (e.g. KomikTap's cdnasu.xyz). Android
	     blocks cleartext by default at targetSdk 28+, which fails those images with "CLEARTEXT communication
	     ... not permitted". Permit cleartext so those sources load, mirroring Doki's network_security_config. -->
	<base-config
		cleartextTrafficPermitted="true"
		tools:ignore="InsecureBaseConfiguration">
		<trust-anchors>
			<!-- Trust preinstalled CAs -->
			<certificates src="system" />

			<!-- Additionally, trust user added CAs -->
			<certificates
				src="user"
				tools:ignore="AcceptsUserCertificates" />
		</trust-anchors>
	</base-config>
</network-security-config>
