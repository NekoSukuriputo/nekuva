package org.nekosukuriputo.nekuva.core

/** App-wide constants. [VERSION_NAME] is the single source for the running version (update check + About). */
object AppInfo {
    const val VERSION_NAME = "1.0.0-beta"
}
